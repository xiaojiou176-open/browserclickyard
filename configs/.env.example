# configs module environment template
# 仅示例模板，不包含任何真实密钥。

# Runtime environment (e.g. development / test / production)
CONFIGS_NODE_ENV=development

# Service/API base URL placeholder
CONFIGS_API_BASE_URL=http://localhost:3000

# Feature flag placeholder
CONFIGS_FEATURE_FLAG=false
