# Reference: Root Governance

Generated from `configs/governance/root-allowlist.yaml`. Do not edit this file manually.

## Allowed Tracked Directories

| Group | Entries |
| --- | --- |
| responsibility | `apps`, `services`, `contracts`, `tooling`, `packages`, `tests` |
| governance | `configs`, `.devcontainer`, `docker`, `docs`, `scripts` |
| vcs | `.github` |
| runtime | `.runtime-cache`, `node_modules` |

## Allowed Tracked Files

`.dockerignore`, `.env.example`, `.gitleaks.toml`, `.gitignore`, `.markdownlint-cli2.yaml`, `.npmrc`, `.pre-commit-config.yaml`, `.secrets.baseline`, `.stylelintrc.json`, `AGENTS.md`, `biome.json`, `CHANGELOG.md`, `CITATION.cff`, `CLAUDE.md`, `CODE_OF_CONDUCT.md`, `commitlint.config.cjs`, `CONTRIBUTING.md`, `DISTRIBUTION.md`, `docker-compose.yml`, `INTEGRATIONS.md`, `justfile`, `LICENSE`, `Makefile`, `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `pyproject.toml`, `README.md`, `SECURITY.md`, `SUPPORT.md`, `tsconfig.json`, `turbo.json`, `uv.lock`, `vercel.json`

## Explicit Exceptions

| Directory | Owner | Reason | Exit Criteria |
| --- | --- | --- | --- |

## Disallowed Runtime Outputs

`.mutmut-cache`, `.coverage`, `.coverage.*`, `.pytest_cache`, `.ruff_cache`, `browserclickyard.egg-info`, `coverage`, `coverage.xml`, `htmlcov`, `mutants`, `playwright-report`, `stryker.log`, `test-results`, `.hypothesis`
