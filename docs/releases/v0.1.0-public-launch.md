# Pagestress v0.1.0 Public Launch Notes

This page is the user-facing release story for the first public Pagestress
surface. It is the canonical note body prepared for the first published GitHub Release
of the public repository.

> Historical launch note:
> the current front door has since been realigned toward the stress-lab-first
> story in `README.md` and `docs/get-started.md`.

## What This Launch Changes

Pagestress now presents itself like a product entrypoint instead of a
governance-only repository:

- The public README leads with a real product screenshot and a fastest-result
  path
- The docs stack now routes by evaluation intent, not only by internal
  governance layer
- The public docs now make the MCP-capable client path legible for Codex,
  Claude Code, and other MCP hosts without turning the product into a generic
  assistant story
- The repo includes a first public proof map, FAQ, and launch-story surface
- The product UI now aligns with the Pagestress brand instead of the historical
  legacy label

## Fastest First Result

If your checkout is already bootstrapped:

```bash
./scripts/dev-up.sh
```

If this is your first run in a fresh checkout:

```bash
./scripts/setup.sh && ./scripts/dev-up.sh
```

Expected outcome:

- Command Center at `http://127.0.0.1:17373`
- API health at `http://127.0.0.1:17380/health/`
- A real operator surface with target inputs, experiment entrypoints, and run parameters

## What Is Public Now

- Public product front door: [README.md](../../README.md)
- Result-first quickstart: [docs/get-started.md](../get-started.md)
- Product positioning: [docs/why-pagestress.md](../why-pagestress.md)
- MCP agent review loop:
  [docs/how-to/mcp-agent-review-loop.md](../how-to/mcp-agent-review-loop.md)
- Public proof surfaces: [docs/proof-center.md](../proof-center.md)
- Public boundary stance: [docs/reference/public-readiness.md](../reference/public-readiness.md)
- Published release entry: [GitHub Releases](https://github.com/xiaojiou176-open/pagestress/releases)

## Verification Notes

These launch surfaces were validated locally during the same implementation pass
and prepared for promotion into a real GitHub Release from `main`:

- `./scripts/setup.sh` now completes successfully in a fresh checkout because
  the repo includes a root `uv.lock`
- `./scripts/dev-up.sh` now reaches local UI and API readiness using the
  repo-safe pnpm entrypoint and path-stable Alembic invocation
- A real local screenshot was captured into
  `docs/assets/pagestress-command-center-real.png`

## Known Constraints

- `pnpm uiq run --profile pr --target web.local` currently requires
  `GEMINI_API_KEY`; without it, the run exits with
  `reasonCode=ai.gemini.strict_policy_violation`
- Runtime evidence and failure bundles remain private-only by default unless a
  path is explicitly allowlisted as public-safe
- AI findings and internal code-quality dashboards are not part of the current
  public machine-verifiable proof set

## Why This Release Matters

The point of this launch is not that Pagestress is finished. The point is that
the repo now gives a much stronger first impression:

- It shows the product before it explains the governance
- It gives evaluators a fastest-result path before deep contracts
- It makes the public proof story easier to inspect and easier to follow
